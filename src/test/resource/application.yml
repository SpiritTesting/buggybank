spring:
    profiles:
        active: test
    datasource:
        url: jdbc:h2:mem:testdb
    jpa:
        show-sql: true
buggybank:
    zinsen:
        delay:
            initial: 50
            rate: 20000